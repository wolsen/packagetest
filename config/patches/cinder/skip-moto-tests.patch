Description: Refresh Ubuntu existing optional-moto test compatibility patch after upstream removed the adjacent eventlet.tpool import. Preserve existing mock_aws/mock_s3 fallback and absence-only skip without adding test exclusions.
Forwarded: not-needed
Author: Ubuntu Developers <ubuntu-devel-discuss@lists.ubuntu.com>
Last-Update: 2026-09-20

--- a/cinder/tests/unit/backup/drivers/test_backup_s3.py
+++ b/cinder/tests/unit/backup/drivers/test_backup_s3.py
@@ -23,9 +23,16 @@
 import tempfile
 import threading
 from unittest import mock
+import unittest
 import zlib
 
-from moto import mock_aws
+try:
+    from moto import mock_aws
+except ImportError:
+    try:
+        from moto import mock_s3 as mock_aws
+    except ModuleNotFoundError:
+        mock_aws = unittest.skip("moto library is not available")
 from oslo_utils import units
 
 from cinder.backup.drivers import s3 as s3_dr
